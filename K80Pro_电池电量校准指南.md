# 红米 K80 Pro 电池电量校准指南（v3）

> **相关数据与分析**：本机电量计各项实测参数（QMax / FCC / SOH / UI SOH 等）见 [电池容量校准与SOH.md](./电池容量校准与SOH.md)。

> 依据说明：以下流程依据本机 MPC8011B 电量计的 TI/BQ 兼容架构以及 Impedance 原理，是目前最有依据的操作法。但 MPC8011B 闭源固件是否完整复用 TI 的 QMax 更新条件，尚无官方资料确认，以下时间/间隔为 TI 兼容算法的保守参考。

## 先分清：你要校准什么

K80 Pro 的电量管理分两层：

1. **电量计芯片层（MPC8011B，TI 类 Impedance Track 电量计）** —— FCC（满充容量）、QMax（化学容量/最大可充电量）、SOC、OCV（开路电压）映射的真正计算都在芯片固件内部完成。
2. **系统显示层（Android 显示的百分比）** —— 系统主要消费电量计上报的 SOC/FCC，再做厂商的 UI 映射、平滑或边界处理；底层容量模型并不由 Android 重新计算。

日常"电量显示不准、跳电"可能与芯片层容量模型（QMax/Ra/OCV）长期未充分学习有关，而非"放空再充满"能直接解决；严重跳电或提前关机还需排除电芯老化与高内阻。普通用户能做的是**创造条件让芯片自己完成学习**，而不是手动改写 QMax——不存在用户能直接强制重写 QMax 的开关。

## 校准的本质：给芯片一次高概率的 QMax 学习机会

以 TI BQ27Z561 的 Impedance Track 为兼容参考，普通 QMax 更新需要**两次合格的 OCV 读数**，分别在一次充/放电活动的前后、电池处于 RELAXED（静置）状态时取得：

- **RELAXED（静置）判定**：电池电压变化率 `dV/dt < 1 µV/s`；传统 OCV 路径下，**停止充电后通常最多约 2 小时、停止放电后通常最多约 5 小时**才能满足。快 OCV（Fast OCV）可缩短，但本机是否启用未确认。
- **两次静置之间必须有足够多的充/放电活动**：容量变化 **≥ 37% 设计容量**。
- **其他否决条件**（TI 兼容参考）：温度需大致在 10–40°C；OCV 若落在 ChemID 的平坦电压区、或累计 offset error 过大，本次更新会作废。这些普通用户通常无法直接控制，因此以下流程只是**创造高概率学习机会，并不保证一定触发 QMax 更新**。

因此，**不要求放到 0% 自动关机，也不要求从 0 充到 100%**。TI 普通 QMax 更新只看"两次合格 OCV 之间容量变化是否够大"。

一次从高 SOC 放电到中低 SOC 的正常放电，还能让芯片在放电过程中跨过多个 DOD 分档点、更新 Ra 阻抗表——这对容量模型同样重要。

## 实操步骤（推荐流程）

### 第一步：从高 SOC 开始并充分静置
1. 先把电量充到较高水平（例如 80%~100%），**充满即可，不必追求某一精确值**。
2. **停止充电并静置**。按 TI 兼容参考，充电后约需最多 2 小时满足 OCV 判据。期间避免操作手机、亮屏和后台高负载——电流升高可能使电量计退出 RELAX（但"物理震动"不是判据，无需担心震动本身）。

> 注意：**保持充电器连接≠静置 OCV**。为尽量确保真正停止充电，建议在充满/终止后**拔掉充电器静置**；芯片实际是按电流/充电状态判定 RELAX，而不是按线缆是否连接判定，因此"插着 30 分钟"并不能算校准。

### 第二步：正常放电一段足够大的容量
3. 拔掉后正常使用放电。**UI 上的 SOC 百分比只是近似代理**，不能直接等同于"通过了 37% 设计容量"（尤其电池已衰减时，40 个百分点的下降可能对应远少于 37% 的实际电量）。为留出余量，普通用户可采用**更大的 SOC 跨度**，例如约 90% 用到 30% 左右；最终是否达到芯片的容量变化门槛，由 FG 内部判断。
4. 期间正常用即可，不需要特意放空或用到关机。

### 第三步：再次静置
在较低 SOC 处（例如 30% 左右）再静置一段时间，尝试取得第二次合格 OCV。放电后的静置通常可能更长（最多约 5 小时参考值）。

### 第四步：正常充满
充电到正常满电并让其自然终止即可。有效充电终止本身会触发 FCC 重算（这只是一次普通重算，不代表 QMax 校准完成）。

### 之后：正常使用并观察
之后正常使用即可。传统双 OCV QMax 路径需要再次满足合格静置和足够容量变化才会更新；Ra 等其他模型更新路径有各自的条件，并不会都等到下一次"两次静置 + 足够容量变化"。因此**不存在"固定 1-2 个周期必然收敛"**。

## 常见误区

- **误区一：必须放到自动关机 / 反复放空充满 3 次。** 不需要。TI 兼容算法不要求 0% 端点；关键是两次静置之间容量变化是否足够大（按近似代理，尽量让 SOC 跨度大一些）。
- **误区二："插着充电器保持 30 分钟" 就是校准。** 那只会触发 FCC 常规重算，不等于 OCV/QMax 学习。
- **误区三：去设置里找"电池校准"入口强制重写。** 本机分析未发现独立的 FCC 校准命令或该入口；不要把不存在的系统功能当步骤。真正的校准是芯片在静置条件下自动完成的。
- **误区四：删除/格式化所谓"电量计学习文件"。** 多数是 sysfs 只读节点，删除无效且可能影响系统。

## 什么时候该考虑换电池

如果完成了上述条件（充分静置 + 足够容量变化）后，仍出现"还有 40% 直接关机"、跳电严重；或系统当前版本提供了电池健康信息且显示容量明显下降、售后检测也确认容量下降，可能是电芯老化或内阻增大而非校准问题，建议官方售后检测/更换。跳电的成因不止校准一项，需一并排除老化和高内阻。

## 一句话总结

K80 Pro 的校准核心不是"放空充 3 次"，而是 **"高电量静置（充电后约 2h）→ 正常放电到较低电量（SOC 跨度尽量大）→ 再静置（放电后约 5h 参考）→ 正常充满"**，在充足静置 + 足够容量变化中给芯片一次高概率的 QMax/OCV 学习机会；具体阈值是否完全沿用 TI 算法，以 MPC8011B 固件为准。