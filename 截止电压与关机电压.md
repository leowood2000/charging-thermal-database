# 截止电压与关机电压数据库（K80 Pro / miro）

> 本文是**电池与充放电策略参数**，不属于有线或无线某一条充电路径：充电终止电压（JEITA vterm）属于**充电策略**（对有线/无线两条路径都生效），放电截止/关机电压由**电量计 `strategy-fg`**决定。有线、无线两份数据库遇到"充到多少伏停"或"放到多少伏关机"时都引用本文。

> **相关文档**：电池**容量**侧（`charge_full`/Qmax 学习、SOH 校准条件）见 [`电池容量校准与SOH.md`](电池容量校准与SOH.md)。

> 数据来源（全部为本机实时读取，2026-09-15 实测，Redmi K80 Pro / `miro`）：
> - 设备树（设备上实际生效的 DTBO，只读）：`/sys/firmware/devicetree/base/soc/strategy-fg/*`、`/sys/firmware/devicetree/base/soc/mca_buckchg_jeita/*`、`/sys/firmware/devicetree/base/soc/mca_vbat_ovp_mon/*`
> - 运行期日志：`dmesg` 中的 `[mca_battery_shutdown]`、`[mca_strategy_fg]`
> - 逻辑实现：`/vendor/lib/modules/mca_strategy_fg_comp.ko`（内含 `mca_battery_shutdown_*` 全部函数）
> - 交叉参考（OSS 源码，**与出厂固件不完全一致**，见第五节）：`xiaomi-smxxx0-devs/kernel_xiaomi_sm8750-devicetrees` 的 `qcom/miro-charger-common.dtsi`

## 零、先说清楚"两个截止电压"

| 名称 | 方向 | 作用 | 本机实测取值 |
|---|---|---|---|
| **充电终止电压** `vterm`（JEITA） | 充电 | 判断"充满"的电压上限；温度越高越低 | 常规 4510~4520 mV，FFC 快充 4550 mV，48 °C 以上降到 4060 mV |
| **放电截止 / 关机电压** `vcutoff` | 放电 | 低于它进入关机流程；随温度和老化抬高 | 常温当前档 **3050 mV**，老化后最高升到 **3340~3350 mV** |

两者数值相近纯属巧合，作用方向完全相反。

## 一、充电侧：JEITA 终止电压 / 终止电流

设备树节点：`/sys/firmware/devicetree/base/soc/mca_buckchg_jeita/jeita_para`
字段顺序：`temp_low, temp_high, low_temp_hys, high_temp_hys, max_current(mA), vterm(mV), iterm(mA), volt_para_name`

### 1.1 常规充电（非 FFC）

| 温度区间 (℃) | 电池侧电流上限 (mA) | vterm (mV) | iterm (mA) |
|---|---:|---:|---:|
| ≤ -9 | 0 | 4530 | 500 |
| -9 ~ 0 | 578 | 4520 | 289 |
| 0 ~ 5 | 1156 | 4520 | 289 |
| 5 ~ 10 | 2890 | 4520 | 289 |
| 10 ~ 15 | 4000 | 4520 | 289 |
| 15 ~ 20 | 4000 | 4520 | 289 |
| 20 ~ 48 | 4000 | **4510** | 289 |
| 48 ~ 60 | 2890 | **4060** | 289 |
| ≥ 60 | 0 | 4060 | 230 |

### 1.2 FFC（快充）覆盖表 `jeita_para_ffc`

| 温度区间 (℃) | 电池侧电流上限 (mA) | vterm (mV) | iterm (mA) |
|---|---:|---:|---:|
| 20 ~ 30 | 4000 | **4550** | 1776 |
| 30 ~ 35 | 4000 | 4550 | 2000 |
| 35 ~ 40 | 4000 | 4550 | 2500 |
| 40 ~ 48 | 4000 | 4550 | 3500 |
| 48 ~ 60 | 2890 | 4060 | 289 |
| ≥ 60 | 0 | 4060 | 230 |

要点：
- 快充只在 **20~48 ℃** 才把终止电压从 4510/4520 抬到 **4550 mV**；进入 48~60 ℃ 高温档后 vterm 降至 **4060 mV**，会显著限制高温状态下允许继续充入的能量。注意：**实际 SOC 显示还受电量计映射与充电状态机影响**，不能仅凭该参数判断"100% 对应多少实际容量"。
- 每个温度档还带 `low_temp_hys` / `high_temp_hys` 迟滞（本机 1~2 ℃），跨档是**带迟滞**的切换，不是单点跳变。
- `iterm` 是终止电流：充电电流降到该值以下持续一段时间才判"充满"。FFC 档把 iterm 放大到 1776~3500 mA，是为了在高压快充下更早收尾。

### 1.3 过压保护（与终止电压配套）

`/sys/firmware/devicetree/base/soc/mca_vbat_ovp_mon/`：

| 属性 | 值 | 含义 |
|---|---:|---|
| `vbat_ovp_threshold_normal` | 4530 mV | 常规充电的过压保护点 |
| `vbat_ovp_threshold_ffc` | 4580 mV | FFC 快充的过压保护点 |

OVP 是**独立配置**，不随 JEITA 表变化：常规档 4530 mV 比常用档 vterm（4510~4520 mV）高 **10~20 mV**；FFC 档 4580 mV 比 4550 mV 高 **30 mV**。

### 1.4 运行时怎么观测
- MCA 投票主题 `term_volt`（单位 mV）、`term_curr`（单位 mA）——就是上表选中的那一行；实时仪表盘里是"JEITA 终止参数"卡。
- 该值由**当前策略档**决定：同一充电会话内通常不变，但会随**电池温度跨档**（含迟滞）与 **FFC / 非 FFC 切换**而变化，因此应理解为"当前策略档参数"，而不是恒定常数。

## 二、放电侧：关机电压

### 2.1 四个全局量（DT 里是 big-endian u32，单位 mV）

| 属性 | 本机实测 | 含义 |
|---|---:|---|
| `vcutoff_fw` | 3050 | FG/FW 侧截止阈值（模块日志里的 `vcutoff_fw`）；**具体触发机制（是否参与硬截止）待驱动逻辑确认** |
| `vcutoff_shutdown_delay` | 3050 | 触发"延迟关机"判定的电压基准 |
| `vcutoff_sw` | 2900 | 全局软件侧 cutoff 参数；当前值与常温各档 `vempty=2900 mV` 相同，但**两者语义是否完全一致尚未确认**（驱动保留了两个独立字段） |
| `fg_hightemp_vterm` | 4200 | 高温下的替代终止电压 |

### 2.2 温度分档 → 关机电压表（`dod_vcutoff_cfg` + `dod_para0~4`）

`dod_vcutoff_cfg` 每行 5 个字段：`temp_low, temp_high, low_hys, high_hys, 表名`，本机实测：

| 温度区间 (℃) | 选用表 |
|---|---|
| -65535 ~ **-6** | `dod_para0` |
| -6 ~ 0 | `dod_para1` |
| 0 ~ 10 | `dod_para2` |
| 10 ~ 60 | `dod_para3` |
| 60 ~ 65535 | `dod_para4` |

各表每行 4 个字段：`dod_count(门槛), vterm_fw, vterm_shutdown_delay, vempty`，单位 mV（`dod_count` 为计数门槛）：

| 表（温度带） | dod_count | vterm_fw | vterm_shutdown_delay | vempty |
|---|---:|---:|---:|---:|
| `dod_para0`（极冷 ≤ -6 ℃） | 121 | 2900 | 2850 | 2750 |
| | 383 | 2900 | 2850 | 2750 |
| | 557 | 3000 | 2950 | 2850 |
| | 65535 | 3050 | 3000 | 2900 |
| `dod_para1`（-6 ~ 0 ℃） | 121 | 2900 | 2850 | 2750 |
| | 383 | 2900 | 2850 | 2750 |
| | 557 | 3050 | 3000 | 2900 |
| | 65535 | 3150 | 3100 | 2900 |
| `dod_para2`（0 ~ 10 ℃） | 121 | 3050 | 3050 | 2900 |
| | 383 | 3050 | 3050 | 2900 |
| | 557 | 3150 | 3150 | 2900 |
| | 65535 | 3250 | 3250 | 2900 |
| `dod_para3`（10 ~ 60 ℃，常温） | 121 | 3050 | 3050 | 2900 |
| | 383 | 3150 | 3150 | 2900 |
| | **557** | **3250** | **3250** | **2900** |
| | 65535 | **3340** | **3340** | 2900 |
| `dod_para4`（≥ 60 ℃） | 65535 | 3340 | 3340 | 2900 |

（用户此前引用的 `dod_para3 = 121 3050 3050 2900 / 383 3150 3150 2900 / 557 3250 3250 2900 / 65535 3340 3340 2900` 与本机实测完全一致。）

### 2.3 循环次数/老化分档（`cc_vcutoff_cfg` + `cc_para0/1`）

`cc_vcutoff_cfg`（温度分档，低于 0 ℃ 与 ≥ 0 ℃ 各一张）：

| 温度区间 (℃) | 选用表 |
|---|---|
| -65535 ~ 0 | `cc_para0` |
| 0 ~ 65535 | `cc_para1` |

| 表（温度带） | 计数门槛 | vcutoff_fw | vcutoff_shutdown_delay | vempty |
|---|---:|---:|---:|---:|
| `cc_para0`（< 0 ℃） | 600 | 2900 | 2850 | 2750 |
| | 1200 | 3100 | 3050 | 2900 |
| | 65535 | 3200 | 3150 | 2900 |
| `cc_para1`（≥ 0 ℃） | 600 | 3050 | 3050 | 2900 |
| | 1200 | **3250** | **3250** | 2900 |
| | 65535 | **3350** | **3350** | 2900 |

### 2.4 关机是怎么触发的（不是"到电压直接断电"）

> 本机 DT 同时存在空属性 `support-dod-vcutoff` 与 `support-cc-vcutoff`（DT 里空属性表示布尔为真），说明 **dod 与 cc 两套截止机制在本机同时启用**，不是二选一。
>
> **但两套机制的最终阈值仲裁规则尚未通过反汇编确认**：例如 dod 档算出 3250 mV、cc 档算出 3050 mV 时，驱动究竟是取较高值、取较低值、分别更新不同用途的字段，还是一个作用于 FW 侧、一个作用于 shutdown delay，目前无法从函数名与 DT 判定。**不能假定"取高/取低/叠加"中的任何一种。**

`mca_strategy_fg_comp.ko` 里的判定链路（函数名取自模块字符串，全部为实测提取）：

```
mca_battery_shutdown_parse_dt            解析 vcutoff_fw / vcutoff_shutdown_delay / vcutoff_sw
mca_battery_shutdown_parse_vcutoff_para  解析 dod_vcutoff_cfg / cc_vcutoff_cfg 与各档表
mca_battery_shutdown_update_vcutoff_para 按当前温度/计数选出生效档（dmesg 可见）
mca_battery_shutdown_check_shutdown_delay 持续比较 pack_vbat 与 shutdown_delay，满足延时才关机
mca_battery_shutdown_heavy_load_check     识别大负载瞬态压降（heavy_load_count），避免误判
mca_shutdown_due_to_vpack_low_of_vpack_inside_fg_absent  FG 内部 vpack 过低时兜底关机
```

要点（**事实**部分有本机证据，**推断**部分已标注）：
1. **事实**：设备树里 `vcutoff_fw` / `vcutoff_shutdown_delay` / `vcutoff_sw` 与各档表独立存在，且 `dod`、`cc` 两套表并存。
2. **事实**：驱动每次更新档位时打印 `temp_para_size:5, temp: 31, high: ..., low: ...`，说明温度带确实是 5 档（-65535/-6、-6/0、0/10、10/60、60/65535）。
3. **推断**：`check_shutdown_delay` 判定"电压持续低于 `vterm_shutdown_delay` 且累积到延时"后才触发关机，`heavy_load_check` 负责滤掉瞬时压降；具体延时值与比较次数未反汇编确认。
4. **推断**：`vempty`（本机 2750~2900 mV）是 FG 的 empty-voltage 相关参数，参与低 SOC / empty 状态计算；**是否直接对应 Android 的 SOC=0%、以及是否等价于"归零点"仍需驱动实现确认**——电量计 SOC 还涉及库仑计、OCV、阻抗模型与 SOC 平滑，并非简单的 `Vbat ≤ vempty → SOC=0`。可作为参考的是：`vempty` 整体低于同档 `vcutoff_*`，因此 **SOC 0% 与关机不是同一时刻**。

### 2.5 所以 3.35 V 是什么

- 3.35 V（3340~3350 mV）是**老化后的常温档关机电压**，不是"电池最低放电电压"，也不是"到点直接断电"。
- 全新状态（本机当前档）关机线是 **3050 mV**；极冷档（≤ -6 ℃）反而更低：`vterm_shutdown_delay` 为 **2850~3000 mV**、`vterm_fw` 为 **2900~3050 mV**。（极冷档表里的 **2750 mV 是 `vempty` 列，不是关机线**——两者不要混。）
- 一块电芯从 3050 mV 抬到 3350 mV，等于可用放电区间被**策略性收窄**（事实）；由此带来"循环次数上去后续航变短"是**工程推断**，不是厂商公开确认的设计目标。

## 三、循环寿命 ↔ 关机电压（对应 `cc_vcutoff`）

`cc_para1`（≥ 0 ℃）本机实测：

| 计数门槛 | vcutoff_fw / vcutoff_shutdown_delay | 相对第一档抬高 |
|---|---:|---:|
| 600 | 3050 | — |
| 1200 | 3250 | +200 mV |
| ≥ 1200（65535 档） | 3350 | +300 mV |

- **事实**：门槛越大，关机电压越高；`dod` 表同理（3050 → 3150/3250 → 3340）。
- **工程推断**（非厂商已证实的设计说明）：抬高 cutoff 可以减少老化电池因内阻增大、负载压降造成的异常掉电（同样电流下老电芯端电压更低，若仍按 3050 mV 判关机，会出现"显示还有电但一加载就掉电/关机"），代价是牺牲一部分可用能量。
- **待确认**：`dod_count`（放电深度累计）与 `cycle_count`（循环次数）在本机读数不一致——2026-09-15 实测 `dod_count = 3`、`cycle_count = 390`，两者必然不同源（可能是不同的累计口径或复位策略），而 `dod_para` 的门槛是 121/383/557，`cc_para` 的门槛是 600/1200，两套门槛体系也不能直接互换。此处不做进一步断言。

## 四、本机验证命令（可直接复现）

```bash
# 1) 放电截止/关机电压：4 个全局量（DT 为大端 u32，用 od 看原始字节）
adb shell "su -c 'for n in vcutoff_fw vcutoff_sw vcutoff_shutdown_delay fg_hightemp_vterm; do echo \$n; od -An -tx1 /sys/firmware/devicetree/base/soc/strategy-fg/\$n; done'"
# 实测：vcutoff_fw 00 00 0b ea(3050)、vcutoff_sw 00 00 0b 54(2900)、vcutoff_shutdown_delay 00 00 0b ea(3050)、fg_hightemp_vterm 00 00 10 68(4200)

# 2) 温度分档表 + 五张 dod 表（DT 字符串表按 NUL 分隔）
adb shell "su -c 'for n in dod_vcutoff_cfg dod_para0 dod_para1 dod_para2 dod_para3 dod_para4 cc_vcutoff_cfg cc_para0 cc_para1; do echo \"### \$n\"; tr \"\0\" \"\n\" < /sys/firmware/devicetree/base/soc/strategy-fg/\$n | grep -v \"^\$\"; done'"

# 3) JEITA 充电终止电压/电流（有线、无线共用）
adb shell "su -c 'tr \"\0\" \"\n\" < /sys/firmware/devicetree/base/soc/mca_buckchg_jeita/jeita_para; echo ===FFC; tr \"\0\" \"\n\" < /sys/firmware/devicetree/base/soc/mca_buckchg_jeita/jeita_para_ffc'"

# 4) 运行期档位选择日志（每 5 s 一次）
adb shell "su -c 'dmesg | grep -a mca_battery_shutdown | tail -5'"

# 5) 电量计实时计数（dod_count / vpack / SOC 小数）
adb shell "su -c 'for n in dod_count pack_vbat soc_decimal soc_decimal_rate fast_charge; do printf \"%s=\" \$n; cat /sys/devices/platform/soc/soc:strategy-fg/\$n; done'"
```

2026-09-15 13:5x 实测快照（放电中）：

| 项目 | 值 |
|---|---|
| `pack_vbat`（FG 端电压） | 3895 mV |
| `voltage_now`（power_supply 上报） | 3911 mV |
| `capacity` / `soc_decimal_rate` | 53 % / 33 |
| `temp` | 278 = 27.8 ℃ |
| `current_now` | 13 mA（放电） |
| `dod_count` | 3 |
| `cycle_count` | 390 |
| `charge_full` / `charge_full_design` | 5797 / 6000 mAh |
| 型号 | `O11U_6000mah_120w` |

按 27.8 ℃ 落进 `10~60 ℃` 档，`dod_para3` + `cc_para1` 的第一档均给出 **3050 mV**——两套表在常温第一档上自洽。

## 五、与 OSS 源码的差异（重要）

`miro-charger-common.dtsi`（`&strategy_fg` 段）能查到字段语义，但**温度带划分与数值都与出厂固件不同步**：

| 项目 | 本机固件 DT（本文采用） | OSS 源码（lineage-22.2） |
|---|---|---|
| 温度带划分 | 5 档：-65535/-6、-6/0、0/10、10/60、60/65535 | 4 档：-65535/0、0/10、10/60、60/65535（**没有 -6~0 ℃ 独立档**） |
| **常温 10~60 ℃ 用哪张表** | `dod_para3`（因多一条 -6~0 档，表名相对 OSS **整体顺延一位**） | `dod_para2` |
| 常温表内容 | 四行：121/3050、383/3150、557/3250、65535/**3340** | 三行：48/3050、136/3150、65535/**3250** |
| **高温 ≥60 ℃ 用哪张表** | `dod_para4` = **3340** | `dod_para3` = **3050** |
| `cc_vcutoff_cfg` | 独立节点（门槛 600/1200/65535） | 内联在 `&strategy_fg`（门槛 800/1200/65535，值也不同） |
| `soc-proportion` | 95 | 97 |

准确的对应关系是：

> 本机新版固件增加了 `-6~0 ℃` 独立温度带，使常温 10~60 ℃ 从 OSS 的 `dod_para2` 顺延成 `dod_para3`；同时本机常温 DOD 门槛也由 OSS 的 48/136/65535 更新为 121/383/557/65535，并增加最高 3340 mV 档。

**注意**：对比两张表必须按"温度带"对齐，**不能按表名对齐**——按表名对齐会把 OSS 的 `dod_para2` 与本机的 `dod_para3` 张冠李戴（本节早期版本就犯过这个错）。

结论：**以设备实测为准**，OSS 源码只用于确认字段顺序与函数语义。

## 六、能不能改？改哪里？（风险提示）

- **不能**靠 sysfs 直接改：`/sys/firmware/devicetree/base/...` 下这些属性全部是 `-r--r--r--`（只读），`/sys/devices/platform/soc/soc:strategy-fg/` 只暴露 `dod_count`、`pack_vbat`、`fake_soc`、`fake_temp` 等运行期量，没有 vcutoff 可写项。
- **改上层没用**：JEITA 与 vcutoff 判定都在内核侧（`mca_strategy_fg_comp.ko`），LSPosed/HAL hook 只能改"显示出来的数字"，改不动实际关机点。
- **能影响的是**：`/odm/etc/charger/BAA_config_miro.json` 里的浮充/终止策略（本仓库《有线充电热控数据库》已记录其作用边界），但它不改变本文的 `vcutoff` 表。
- **真要改**需要动内核模块或 DTBO 并绕过 AVB/verity 校验（本机未验证，属常规做法）。风险：把 vcutoff 调低会造成过放、加速老化并可能触发低电压保护；调高则牺牲可用容量。**不建议以"延长续航"为目的下调关机电压。**
